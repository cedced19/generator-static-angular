# <%= title %>

<%= description %>

Install dependencies:
```bash
$ npm install -g bower
$ npm install -g gulp
$ npm installl
$ bower install
```

To test:
```bash
$ gulp serve
```

To compile:
```bash
$ gulp
```

See `gulpfile.js` for task config.
